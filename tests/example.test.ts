import { describe, expect, test } from 'vitest'

describe('Teste de exemplo', () => {

    test('Meu caso de teste', () => {
        // Arrange - Preparação
        // Act - Execução
        // Assert - Verificação
        expect(1 + 1).toBe(2);
    })
}); 